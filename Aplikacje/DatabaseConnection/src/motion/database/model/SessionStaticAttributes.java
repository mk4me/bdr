/**
 * 
 */
package motion.database.model;

public enum SessionStaticAttributes{
	sessionID,
    userID,
    labID,
    motionKindID,
    performerID,
    sessionDate,
    sessionDescription;
}