/**
 * 
 */
package motion.database;

public enum SessionStaticAttributes{
	sessionID,
    userID,
    labID,
    motionKindID,
    performerID,
    sessionDate,
    sessionDescription;
}